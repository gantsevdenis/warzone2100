#include "astar2.h"
#include "map.h"

*-*/-
#include <list>
#include <vector>
#include <algorithm>
#include <memory>

// suppose we wan to split map into 16x16 cells
static unsigned nbCells = 0xFFFFFFFF;
ASR_RETVAL fpathAStar2Route(MOVE_CONTROL *psMove, PATHJOB *psJob)
{

    printf("mapHeight %i", mapHeight);
}
dsvdsv