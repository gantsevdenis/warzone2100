#ifndef _stateregistry_h_
#define _stateregistry_h_

#include <stdlib.h>
#include <unordered_map>
#include "../basedef.h"
#include "fsm.h"
/*namespace FSM
{
    class StateRegistry
    {
        public:
        const void getMoveLikeState(uint32_t uid)
        {
            //return moveStates[uid];
        }
        void getAttackLikeState(uint32_t uid)
        {

        }
        private:
        //std::unordered_map <uint32_t, MoveBase&> moveStates;
    }
}
*/
#endif